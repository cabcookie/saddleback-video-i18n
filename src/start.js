'use strict';import createCompsFromTextFile from './functions/create-comps-from-text-file';// Open a dialog and ask for a file.var newFile = File.openDialog("Please choose the file with the relevant texts", "txt");// If we have a file,if (newFile !== null) {	app.beginUndoGroup("Import Text");	createCompsFromTextFile();	app.endUndoGroup();}