'use strict';

export default function checkLineNumsInTemplates(templates) {
    // TODO: Move these checks into this function and save it either in the template or the configuration
    return true;
}
